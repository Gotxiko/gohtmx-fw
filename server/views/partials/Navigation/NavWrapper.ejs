<header
    class="align-center flex w-full gap-4 border-b border-gray-500 bg-gray-600 p-2"
>
    <% for(let navItem of navMenuItems) { %> <%- include('NavItem', { navItem:
    navItem }) %> <% } %>
</header>
