<header class="align-center flex w-full justify-center gap-8 py-4">
    <% for(let navItem of navMenuItems) { %>
    <a
        href="<%=navItem.url%>"
        class="rounded-full bg-blue-500 px-8 py-2 text-white hover:bg-blue-700"
    >
        <%=navItem.name%>
    </a>
    <% } %>
</header>
